{
  "name": "@kitchen-bots/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "npm run typecheck && npm run test",
    "dev": "wrangler dev",
    "deploy": "npm run check && wrangler deploy",
    "deploy:dry-run": "npm run check && wrangler deploy --dry-run"
  },
  "dependencies": {
    "hono": "^4.13.8",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260922.1",
    "typescript": "^5.9.3",
    "vitest": "^5.0.1",
    "wrangler": "^4.136.2"
  }
}
